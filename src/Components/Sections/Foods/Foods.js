import React from 'react';
import SectionHeader from '../../SectionHeader/SectionHeader';

const Foods = () => {
    return (
        <div>
            <SectionHeader
                heading="Choose From Most Popular"
                subHeading="Explore restaurants, bars, and cafés by locality"
            ></SectionHeader>
        </div>
    );
};

export default Foods;